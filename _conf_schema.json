{
  "enable": {
    "description": "启用知识库检索",
    "type": "bool",
    "default": true
  },
  "decision_mode": {
    "description": "触发决策模式 (L1=总是发送, L2=关键词判断, L3=大模型意图识别)",
    "type": "string",
    "options": ["L1", "L2", "L3"],
    "default": "L1"
  },
  "keywords": {
    "description": "L2模式: 触发关键词 (逗号分隔)",
    "type": "string",
    "default": "请问,解释,文档,是什么,介绍,怎么,如何,帮我,可以,帮"
  },
  "custom_llm_provider": {
    "description": "L3决策/重写 LLM (留空则默认为用户正在使用的模型)",
    "type": "string",
    "default": "",
    "_special": "select_provider"
  },
  "l3_prompt": {
    "description": "L3模式: 意图识别提示词",
    "type": "text",
    "default": "你是一个意图识别引擎。\n用户语境:\n{history}\n用户当前输入: {user_msg}\n判断用户上述输入是否是在提问查资料并需要通过外部知识库或文档检索进一步的背景知识。如果需要检索额外资料，回答YES，否则如果只是日常聊天或不需要检索资料，回答NO。只输出YES或NO。"
  },
  "enable_query_rewrite": {
    "description": "启用: 查询重写(解决多轮对话指代问题)",
    "type": "bool",
    "default": true
  },
  "rewrite_prompt": {
    "description": "查询重写系统提示词",
    "type": "text",
    "default": "根据以下对话历史，将用户的最新输入重写为独立的搜索查询词。核心任务是：解决指代不明，将代词(他,那个等)替换为具体提及的实体；如果不需要重写，直接输出用户输入内容。只输出重写后的内容，不要作答或解释。\n\n对话历史:\n{history}\n\n用户最新的需重写输入: {user_msg}"
  },
  "knowledge_prompt_template": {
    "description": "知识检索结果注入系统提示词",
    "type": "text",
    "default": "\n\n请参考以下背景知识来回答用户的问题（如果背景知识与问题无关可以忽略）：\n{context_str}\n\n请结合以上背景知识回答问题。\n"
  },
  "backend_type": {
    "description": "知识库类型",
    "type": "string",
    "default": "dify",
    "options": [
      "dify",
      "ragflow",
      "flowise"
    ]
  },
  "api_endpoint": {
    "description": "API 基地址",
    "type": "string",
    "default": "https://api.dify.ai/v1",
    "hint": "Dify: https://api.dify.ai/v1 | RAGFlow: http://你的域名 | Flowise: https://cloud.flowiseai.com/api/v1"
  },
  "api_key": {
    "description": "API Key",
    "type": "string",
    "default": "",
    "hint": "对应后端的 API 密钥"
  },
  "dataset_id": {
    "description": "知识库 ID",
    "type": "string",
    "default": "",
    "hint": "Dify: Dataset ID | RAGFlow: Dataset ID(多个用逗号隔开) | Flowise: Document Store ID"
  },
  "top_k": {
    "description": "检索数量 (Top K)",
    "type": "int",
    "default": 3,
    "hint": "设置最多返回几个片段"
  },
  "score_threshold": {
    "description": "最小匹配相似度",
    "type": "float",
    "default": 0.5,
    "hint": "分数阈值, 0.0 到 1.0 (如果使用 Flowise, 此项请设为 0)"
  }
}